{
  "Python": {
    "1": {
      "title": "Hello World",
      "description": "Print 'Hello, World!' to the console.",
      "testCases": [
        { "input": "", "output": "Hello, World!", "hidden": false }
      ],
      "hint": "Use print('Hello, World!')",
      "condition": { "mustUse": ["print"] }
    },
    "2": {
      "title": "Dynamic Addition",
      "description": "Read two integers from input (one per line) and print their sum.",
      "testCases": [
        { "input": "3 5", "output": "8", "hidden": false }
      ],
      "hint": "Use input() and int(). Remember to print the sum.",
      "condition": { "mustUse": ["int", "input", "print", "+"] }
    },
    "3": {
      "title": "Even or Odd",
      "description": "Read a number from input and print 'Even' if it's even, or 'Odd' if it's odd.",
      "testCases": [
        { "input": "2", "output": "Even", "hidden": false }
      ],
      "hint": "Use the modulo operator (%) and if/else.",
      "condition": { "mustUse": ["%", "if"] }
    },
    "4": {
      "title": "Square Loop",
      "description": "Read a number N and print the squares of all numbers from 1 to N, each on a new line.",
      "testCases": [
        { "input": "3", "output": "1 4 9", "hidden": false }
      ],
      "hint": "Use range(1, N+1) and a for loop.",
      "condition": { "mustUse": ["for", "range"] }
    }
  },
  "Java": {
    "1": {
      "title": "Hello World",
      "description": "Print 'Hello, World!' to the console.",
      "testCases": [
        { "input": "", "output": "Hello, World!", "hidden": false }
      ],
      "hint": "Use System.out.println()",
      "condition": { "mustUse": ["System.out.println"] }
    },
    "2": {
      "title": "Scanner Addition",
      "description": "Use a Scanner to read two integers and print their sum.",
      "testCases": [
        { "input": "4 6", "output": "10", "hidden": false }
      ],
      "hint": "Use scanner.nextInt() twice.",
      "condition": { "mustUse": ["Scanner", "nextInt"] }
    }
  },
  "C++": {
    "1": {
      "title": "Hello World",
      "description": "Print 'Hello, World!' to the console.",
      "testCases": [
        { "input": "", "output": "Hello, World!", "hidden": false }
      ],
      "hint": "Use std::cout << 'Hello, World!' << std::endl;",
      "condition": { "mustUse": ["cout"] }
    },
    "2": {
      "title": "Cin Addition",
      "description": "Read two integers using cin and print their sum.",
      "testCases": [
        { "input": "5 7", "output": "12", "hidden": false }
      ],
      "hint": "Use cin >> a >> b;",
      "condition": { "mustUse": ["cin", "+"] }
    }
  }
}
